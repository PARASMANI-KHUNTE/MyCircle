const POST_TYPES = ['job', 'sell', 'rent', 'barter', 'service', 'request'];

const ITEM_CATEGORIES = ['electronics', 'other'];

const POST_STATUSES = ['active', 'inactive', 'sold', 'completed', 'archived'];

const EXCHANGE_PREFERENCES = ['money', 'barter', 'flexible'];

module.exports = {
    POST_TYPES,
    ITEM_CATEGORIES,
    POST_STATUSES,
    EXCHANGE_PREFERENCES,
};